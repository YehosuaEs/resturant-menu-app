import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project3-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
